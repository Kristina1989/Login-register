import React from 'react';
import LoginInput from "../Components/LoginInput";

const LoginPage = () => {
    return (
        <div>
            <LoginInput/>
        </div>
    );
};

export default LoginPage;